stages:
  - test
  - docker_build
test:
  image: python:2.7
  stage: test
  script:
    - make deps
    - make lint
    - make test
    - coveralls
docker:
  image: docker:stable
  services:
    - docker:dind
  stage: docker_build
  script:
    - apk add --update make
    - make docker_build
    - make docker_push
